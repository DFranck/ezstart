# @ezstart/typescript-config

Centralized TypeScript configurations (base, Next.js, library) for all apps and packages.

## Install

`pnpm add -D @ezstart/typescript-config`

## Usage

```json
// tsconfig.json
{ "extends": "@ezstart/typescript-config/nextjs" }
```

## Used by

All apps and packages.

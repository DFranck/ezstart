/**
 * Stable public interface for the auth-sdk component showcase registry.
 *
 * The real content lives in `auth-sdk-registry.generated.ts` (gitignored,
 * rebuilt by `turbo run generate`). This file is committed so consumers
 * keep a stable import path regardless of regeneration.
 */
export * from './auth-sdk-registry.generated.js'

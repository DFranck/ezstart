'use client'

import { Placeholder } from './Placeholder'

export default function Demo() {
  return (
    <Placeholder
      name="ConnectFeeSummary"
      reason="Read-only summary of the application fee structure for a Connected Account (percentage, fixed fee, currency)."
    />
  )
}

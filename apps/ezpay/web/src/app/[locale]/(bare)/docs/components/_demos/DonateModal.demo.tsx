'use client'

import { Placeholder } from './Placeholder'

export default function Demo() {
  return (
    <Placeholder
      name="DonateModal"
      reason="Hosted donation flow inside a modal — amount picker, currency selector, optional message field, and a Stripe Checkout redirect on submit. Use when you want the donation flow without leaving the page."
    />
  )
}

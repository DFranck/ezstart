'use client'

import { Placeholder } from './Placeholder'

export default function Demo() {
  return (
    <Placeholder
      name="DonationCard"
      reason="Self-contained card combining a project pitch, suggested amounts, and a DonateButton. Drop on landing pages or sidebars to convert visitors without a separate flow."
    />
  )
}

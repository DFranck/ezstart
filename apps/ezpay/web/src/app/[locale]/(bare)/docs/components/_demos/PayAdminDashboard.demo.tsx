'use client'

import { Placeholder } from './Placeholder'

export default function Demo() {
  return (
    <Placeholder
      name="PayAdminDashboard"
      reason="Federated EZPay admin dashboard — overview, payments, subscriptions, plans, promo codes, top apps. Mounted by ezstart hub for superadmins."
    />
  )
}

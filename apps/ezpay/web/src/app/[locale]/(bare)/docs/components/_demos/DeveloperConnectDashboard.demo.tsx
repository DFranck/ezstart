'use client'

import { Placeholder } from './Placeholder'

export default function Demo() {
  return (
    <Placeholder
      name="DeveloperConnectDashboard"
      reason="Federated dashboard for developer-owned Connect Accounts — list of connected accounts, payouts, and webhook health."
    />
  )
}

'use client'

import { Placeholder } from './Placeholder'

export default function Demo() {
  return (
    <Placeholder
      name="ManageSubscriptionButton"
      reason="Single-button launcher for the Stripe Customer Portal — handles return URL + the implicit logout-on-return edge case."
    />
  )
}

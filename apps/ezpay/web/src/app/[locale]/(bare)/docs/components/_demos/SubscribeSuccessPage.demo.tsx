'use client'

import { Placeholder } from './Placeholder'

export default function Demo() {
  return (
    <Placeholder
      name="SubscribeSuccessPage"
      reason="Subscription success landing — renders plan summary, next renewal date, and onboarding CTAs (link to dashboard, etc.)."
    />
  )
}

'use client'

import { Placeholder } from './Placeholder'

export default function Demo() {
  return (
    <Placeholder
      name="SubscriptionPlanCard"
      reason="Plan presentation card used inside PricingPage — features list, price, period, and a Subscribe button. Composable abstraction over @ezstart/ui Card."
    />
  )
}

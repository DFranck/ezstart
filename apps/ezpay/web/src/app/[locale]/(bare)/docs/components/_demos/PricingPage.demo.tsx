'use client'

import { Placeholder } from './Placeholder'

export default function Demo() {
  return (
    <Placeholder
      name="PricingPage"
      reason="End-to-end pricing page — auto-fetches plans from EZPay, renders a SubscriptionPlanCard grid, handles billing-cycle toggle (monthly/annual). Required prop: applicationId."
    />
  )
}

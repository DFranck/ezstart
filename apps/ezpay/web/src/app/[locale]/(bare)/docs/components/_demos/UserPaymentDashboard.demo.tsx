'use client'

import { Placeholder } from './Placeholder'

export default function Demo() {
  return (
    <Placeholder
      name="UserPaymentDashboard"
      reason="End-user dashboard combining the user's active subscription, payment history, and donation history in one tabbed view."
    />
  )
}

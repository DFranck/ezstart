'use client'

import { Placeholder } from './Placeholder'

export default function Demo() {
  return (
    <Placeholder
      name="ConfirmActionDialog"
      reason="Generic confirm dialog (title, body, confirm + cancel labels) — useful for destructive subscription actions like 'Cancel plan'."
    />
  )
}

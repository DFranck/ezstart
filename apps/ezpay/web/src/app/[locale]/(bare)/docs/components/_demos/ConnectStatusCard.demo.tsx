'use client'

import { Placeholder } from './Placeholder'

export default function Demo() {
  return (
    <Placeholder
      name="ConnectStatusCard"
      reason="Card displaying the current Stripe Connect onboarding status of a Connected Account (incomplete / pending / verified) with a CTA to resume onboarding."
    />
  )
}

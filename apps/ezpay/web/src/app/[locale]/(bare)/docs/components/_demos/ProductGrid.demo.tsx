'use client'

import { Placeholder } from './Placeholder'

export default function Demo() {
  return (
    <Placeholder
      name="ProductGrid"
      reason="Responsive grid of ProductCards with built-in filter + sort controls. Driven by ProductGridFilterOptions."
    />
  )
}

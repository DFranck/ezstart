'use client'

import { Placeholder } from './Placeholder'

export default function Demo() {
  return (
    <Placeholder
      name="ProductCard"
      reason="Lower-level abstraction used by ProductGrid — image header, title, price, optional badge, and an action slot."
    />
  )
}

'use client'

import { Placeholder } from './Placeholder'

export default function Demo() {
  return (
    <Placeholder
      name="DonateSuccessPage"
      reason="Pre-built success landing — renders a friendly confirmation, optional thank-you message, and an auto-redirect after a configurable delay."
    />
  )
}

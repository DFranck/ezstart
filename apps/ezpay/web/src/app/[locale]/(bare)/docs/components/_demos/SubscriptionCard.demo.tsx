'use client'

import { Placeholder } from './Placeholder'

export default function Demo() {
  return (
    <Placeholder
      name="SubscriptionCard"
      reason="Compact card surfacing the user's active subscription — plan name, renewal date, status, with quick actions (manage / cancel)."
    />
  )
}

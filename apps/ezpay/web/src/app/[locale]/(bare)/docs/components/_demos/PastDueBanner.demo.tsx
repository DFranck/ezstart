'use client'

import { Placeholder } from './Placeholder'

export default function Demo() {
  return (
    <Placeholder
      name="PastDueBanner"
      reason="Top-of-page warning banner shown when the user's subscription is in past_due. Includes an inline Update payment CTA."
    />
  )
}

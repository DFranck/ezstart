# ASC-TCD Web Configuration Template
# Copy to .env.local and fill with real values

# Server Configuration
PORT=5055

# External Services (if needed)
# Add any external APIs or services used by ASC-TCD
# Currently standalone, no external dependencies